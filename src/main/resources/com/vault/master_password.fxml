<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.text.Font?>

<!-- 
    The root VBox container. 
    fx:controller binds this UI strictly to the MasterPasswordController class. 
-->
<VBox fx:id="mainContainer" 
      alignment="CENTER" 
      spacing="20.0" 
      xmlns="http://javafx.com/javafx/21" 
      xmlns:fx="http://javafx.com/fxml/1" 
      fx:controller="com.vault.ui.MasterPasswordController">
      
    <padding>
        <Insets bottom="40.0" left="40.0" right="40.0" top="40.0" />
    </padding>

    <!-- Header / Logo Area -->
    <Label text="🔒 LOCAL VAULT" textFill="#bd93f9">
        <font>
            <Font name="System Bold" size="24.0" />
        </font>
    </Label>

    <Label text="Enter master password to unlock" textFill="#f8f8f2" />

    <!-- 
        The Password field. 
        fx:id="passwordField" links directly to the @FXML field in the Controller.
        onAction="#handleUnlock" means pressing the ENTER key will trigger the unlock method!
    -->
    <PasswordField fx:id="passwordField" 
                   promptText="Master Password" 
                   onAction="#handleUnlock"
                   prefHeight="40.0" />

    <!-- 
        The Unlock Button.
        onAction="#handleUnlock" links the click event to the Controller's method.
        styleClass applies CSS styling (which we'll add later in dark-theme.css).
    -->
    <Button mnemonicParsing="false" 
            onAction="#handleUnlock" 
            prefHeight="40.0" 
            prefWidth="Infinity" 
            text="UNLOCK VAULT" 
            styleClass="primary-button" />

    <!-- 
        Error Label.
        Hidden by default. The Controller makes it visible if decryption fails.
    -->
    <Label fx:id="errorLabel" 
           text="Decryption failed. Wrong password?" 
           textFill="#ff5555" 
           visible="false" />

</VBox>
